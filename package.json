{
  "name": "dmp-platform-root",
  "version": "1.0.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r gateway/requirements.txt",
    "dev:gateway": "cd gateway && python -m uvicorn main:app --reload --port 8000",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8001",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -k \"npm run dev:gateway\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}